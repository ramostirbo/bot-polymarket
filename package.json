{
  "name": "polybot",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --minify",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "generate-key": "bun run src/generate-key.ts"
  },
  "dependencies": {
    "@polymarket/clob-client": "^4.15.0",
    "alchemy-sdk": "^3.5.6",
    "drizzle-orm": "^0.41.0",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "latest",
    "@types/bun": "^1.2.9",
    "@types/node": "latest",
    "drizzle-kit": "^0.30.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
