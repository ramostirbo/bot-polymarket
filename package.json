{
  "name": "polybot",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --minify",
    "dev": "bun --watch src/index.ts",
    "drizzle": "npm run drop-db & drizzle-kit generate",
    "drop-db": "bun ./drizzle/reset-db.ts",
    "generate-key": "bun run src/generate-key.ts"
  },
  "dependencies": {
    "@polymarket/clob-client": "^4.15.0",
    "alchemy-sdk": "^3.5.6",
    "drizzle-orm": "^0.41.0",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "latest",
    "@types/bun": "latest",
    "@types/node": "latest",
    "drizzle-kit": "^0.30.6",
    "postgres": "^3.4.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
